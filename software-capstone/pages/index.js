import Head from 'next/head';

const Home = () => (
    <div>
        <Head>
            <title>Donneil's Capstone</title>
            <link rel="icon" href="/favicon.ico" />
        </Head>
    </div>
);

export default Home;
